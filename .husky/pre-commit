#!/bin/sh
. "$(dirname "$0")/_/husky.sh"


echo "💢 Let's start by checking your code before you make a commit 💤"

npx lint-staged

echo "💥Everything looks good, you can proceed with the commit... 😎"
